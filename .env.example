# Application
ECHOMIND_ENVIRONMENT=development
ECHOMIND_LOG_LEVEL=INFO
DOCUMENTS_PATH=/data/documents
EXTRACTIONS_PATH=/data/extractions
API_PORT=8000

# Structured extraction. Set these only when you intentionally use the OpenAI provider.
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=

# Semantic retrieval of extracted claims.
EMBEDDING_PROVIDER=openai
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_DIMENSIONS=1536
REFLECTION_PROVIDER=openai
# Defaults to OPENAI_MODEL when omitted.
OPENAI_REFLECTION_MODEL=
REFLECTIONS_PATH=data/reflections

# Neo4j (the defaults match docker-compose.yml)
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=echomind-local-password

# Optional host-port overrides for Docker Compose
NEO4J_BOLT_PORT=7687
NEO4J_HTTP_PORT=7474

# Single public entrypoint for the PWA and API reverse proxy
APP_PORT=8080
# Comma-separated development hostnames accepted by Vite behind Nginx
VITE_ALLOWED_HOSTS=localhost

# Required for publicly exposed deployments. Keep these only in .env.local.
ECHOMIND_AUTH_USERNAME=
ECHOMIND_AUTH_PASSWORD=
# Generate with: openssl rand -hex 32
ECHOMIND_AUTH_SESSION_SECRET=
ECHOMIND_AUTH_SESSION_TTL_SECONDS=604800
ECHOMIND_AUTH_COOKIE_SECURE=true
